{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults plus the narrow window APIs the embedded player calls. Player commands themselves are app-crate commands registered through generate_handler!, which the ACL does not gate — only core APIs and plugin commands need entries here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "updater:default",
    "process:default"
  ]
}
