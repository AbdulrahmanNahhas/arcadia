# v0.3.5 — Revamp: Linux-native shell, two-mode UI, and the Big-Picture D-Pad

> **Status:** In progress — Phase A done, lint debt cleared, T1/T2 landed (2026-09-14). Next up:
> **Phase E** (see the [status board](#status-board)). Written 2026-09-09 from a full read of the
> working tree at `9952cc7`; findings re-verified at `e1b5bdd`. Tick checkboxes as work lands;
> set each phase's status to `Done (YYYY-MM-DD)` when its acceptance checks pass.
>
> **Read [`AGENT-CONTEXT.md`](./AGENT-CONTEXT.md) before touching code.** It holds every fact an
> agent otherwise re-derives each session (paths, versions, commands, gotchas, house rules). This
> file is the *plan*; that file is the *map*. Update the map whenever a phase moves a file.
>
> **Release theme:** *one application, two postures, native speed.* The Tauri build on Linux is
> the primary product and today it is slow, its text renders badly, and it loads in stages. This
> release is a licensed full revamp: rewrite whole files, delete dead code, reorder directories —
> but keep the visual identity (simple, clean, a little glassy, identical on every page) rather
> than redesigning it. Arcadia keeps the desk experience and gains a **Display Mode** — a
> lean-back, fully D-pad-driven surface built for a TV ten feet away.
>
> **Forward constraint:** Linux (NixOS/Fedora) is the only target *now*. Android and iOS are
> future targets: nothing here may add a Linux-only dependency to the web app, and Rust
> platform code stays behind `cfg(target_os)` boundaries (Phase H). Assume a future device with
> ~1/6 the CPU of the dev laptop, no mouse, and a Chromium System WebView.
>
> **Design direction (binding for every phase):**
> - *Simple and clean.* Fewer surfaces, fewer borders, more whitespace. One page header, one
>   section header, one card, one empty state — from `components/patterns/`.
> - *A little glass.* Floating chrome (top bar, bottom tab bar, popovers, dialogs, action trays)
>   is `bg-<surface>/70 backdrop-blur-xl` with a hairline border. Scrolling content and cards are
>   flat and opaque. Blur is never on anything that scrolls or animates continuously, and is
>   stripped entirely under `tv:` (Display Mode).
> - *Identical everywhere.* Archive, Platform, Admin, Awards, Entities share one shell rhythm,
>   one spacing ladder, one type scale, one motion vocabulary (`--selection-*`, `--layer-*`).
> - *Clean animation.* Opacity and transform only, 150–250 ms, `--selection-ease`. No blur or
>   shadow transitions. Nothing animates on scroll.
>
> **Model guidance (to keep usage in budget):** each phase row in
> [Sequencing](#sequencing-and-acceptance) names the cheapest model that can do it well. Rule of
> thumb: *design decisions, SQL/visibility correctness, Rust/GTK* → Opus 5 Medium; *mechanical
> splits, migrations to factories, class-name audits, string extraction* → Sonnet 5 High;
> *docs, checklists, small fixes* → Sonnet 5 Medium.

---

## Table of contents

Phases are listed in **execution order** (the order they must ship in), not alphabetically —
the letters are stable identifiers from the original audit and are kept so checkboxes, commits,
and conversations stay consistent.

0. [Status board](#status-board)
1. [Findings — what the audit turned up](#1-findings)
2. [Execution order and why](#sequencing-and-acceptance)
3. [Phase A — Correctness fixes](#phase-a--correctness-fixes) · **done**
4. [Phase T — The Tauri shell on Linux](#phase-t--tauri-shell) · T1/T2 landed, T3/T4 open
5. [Phase E — File-size and structure cleanup](#phase-e--structure)
6. [Phase G — TanStack modernization](#phase-g--tanstack)
7. [Phase D — Performance](#phase-d--performance)
8. [Phase C — The focus engine rebuild](#phase-c--focus-engine)
9. [Phase B — Two modes: Normal and Display](#phase-b--two-modes)
10. [Phase F — UI/UX unification on shadcn](#phase-f--uiux-unification)
11. [Phase H — Android TV readiness](#phase-h--android-tv)
12. [Global acceptance checks](#global-acceptance)
13. [Ideas considered and deliberately deferred](#ideas-deferred)
14. [References](#references)

---

<a id="status-board"></a>

## Status board

Update this table when a phase changes state; the checkboxes inside each phase are the detail.

| Phase | State | Landed | Model that did it / should do it | Notes |
| --- | --- | --- | --- | --- |
| **A** Correctness | ✅ Done | 2026-09-13 (`eabd81d`) | Opus 5 | A8 verified in the shell by hand (opaque window + native `backgroundColor`) |
| **Lint zero** | ✅ Done | 2026-09-14 | Sonnet 5 subagents, Opus 5 orchestrating | 481 oxlint + 6 biome → 0. Three rule decisions plus a **shrink-only Phase E allowlist** in `oxlint.config.ts` (see §1.0) |
| **T** Shell | 🟡 T1/T2 landed | 2026-09-13 | Opus 5 | `diagnostics.rs`: `--devtools`, `ARCADIA_LOG`, startup milestones. T1 numbers still to be read off a run; T3 (native Wayland) and T4 (packaging) open |
| **E** Structure | ⬜ Next | | **Sonnet 5 High** | E1 split plan + E2 delete `compat.ts` |
| **G** TanStack | ⬜ | | **Sonnet 5 High** | first checkbox (router context) already done in A7 |
| **D** Performance | ⬜ | | Opus 5 for D1a/D1b; Sonnet 5 High for the rest | |
| **C** Focus engine | ⬜ | | Opus 5 | |
| **B** Display Mode | ⬜ | | Opus 5 for B1/B4; Sonnet 5 High for `tv:` variants | |
| **F** UI unification | ⬜ | | Sonnet 5 High | |
| **H** Android | ⬜ | | Opus 5 | can run alongside 8–10 |

---

<a id="1-findings"></a>

## 1. Findings

Everything below was verified against the tree, not inferred. File references are
`path:line` at commit `9952cc7`; items struck through were resolved later and are kept as the
record of *why* the phase exists.

### 1.0 Lint debt (added 2026-09-13, resolved the same day)

The baseline carried 481 oxlint errors and 6 biome errors, so `pnpm check` could not act as a
gate. Cleared in one sweep (see the status board). Four decisions were taken in `oxlint.config.ts`
rather than in code, each with its rationale inline:

- `no-await-in-loop` off — sequential SQL inside transactions and import scripts is deliberate.
- `oxc/no-map-spread` off — immutability over micro-optimisation.
- `apps/web/src/components/ui/**` ignored — vendored shadcn output, regenerated by the CLI.
- A **Phase E legacy allowlist** (`overrides`): the 33 oversized files E1/E2 split or delete keep
  their untyped-row idiom (`Record<string, unknown>` SQL rows, v1 shape adapters, draft-sync
  effects) until the split rewrites them, so the anti-slop rules and `react/set-state-in-effect`
  are relaxed *only there*. 265 of the original errors live in those files. **The list may only
  shrink**: a file leaves it in the same commit that replaces it, and E's per-file acceptance is
  "lint clean with no override". Everything outside the list is at zero and stays there.

The TypeScript target moved to ES2023 (`toSorted` etc.) as part of the sweep.

### 1.1 ~~Light mode is structurally broken~~ — fixed in A1–A3 (2026-09-13)

- `apps/web/src/routes/__root.tsx:70` renders `<body className="dark">` — hardcoded, never removed.
- `apps/web/src/routes/__root.tsx:62` and `apps/web/src/features/profiles/settings-page.tsx:78`
  both toggle `.dark` on `document.documentElement`.
- `apps/web/src/styles.css:8` defines `@custom-variant dark (&:is(.dark *))`, and
  `styles.css:155` puts every dark token under `.dark`.

Because `body` permanently carries `.dark`, every element in the tree is a descendant of a `.dark`
element, so both the CSS variables *and* every `dark:` utility resolve dark regardless of the
preference. Choosing "فاتح" in settings persists correctly, flips `html.dark` correctly, and
changes nothing visible. The light palette in `styles.css:61-149` has never actually rendered.

### 1.2 There are two competing focus systems

| System | Where | How it registers |
| --- | --- | --- |
| **Explicit** | `features/platform/components/work-card.tsx:100` via `useSpatialFocusable` | React hook, per component |
| **Automatic** | `features/platform/spatial-navigation.tsx:140-244` (`AutomaticSpatialTargets`) | `MutationObserver` on `document.body` → `querySelectorAll` sweep → `SpatialNavigation.addFocusable` per node |

The automatic scanner runs on **every DOM mutation anywhere in the document**, subtree-wide, plus
an attribute filter on five attributes. On each pass it re-queries six selectors across the whole
document, diffs a `Map<HTMLElement, string>`, and calls `getClientRects()` on every candidate —
that is a forced synchronous layout per candidate, per scan. On a laptop this is invisible. On an
Android TV WebView, a rail that lazily swaps forty poster `<img>` elements will trigger dozens of
full-document scans with a layout flush each.

It is also *disabled on exactly the routes an admin uses* (`spatial-navigation.tsx:181-182` skips
`/admin` and `/player/`), which means admin is already keyboard-only-by-accident rather than by
design — useful, because Display Mode wants admin hidden anyway.

### 1.3 There are four different "this is focused" visual treatments

1. `styles.css:331` — global `outline: 3px solid var(--primary); outline-offset: 4px` for
   `[data-spatial-auto][data-focused]`.
2. `work-card.tsx:160` — `ring-[3px] ring-primary ring-offset-2` plus `scale-[1.04]` and
   `-translate-y-1`.
3. `components/ui/button.tsx:8` — Base UI's `focus-visible:border-ring focus-visible:ring-[3px]
   ring-ring/50`.
4. Ad-hoc `focus-visible:` rules in 20 further files (`player-controls.tsx` has 7,
   `sidebar.tsx` has 5, `studio-lineage-page.tsx` has 3).

None of these is a filled, high-contrast selection the way Steam Big Picture, Netflix, or the
Android TV leanback guidance describe it. All four are *outlines on an unchanged surface*, which
is precisely the treatment that stops reading from three metres away.

### 1.4 The API pages in JavaScript, not SQL

`apps/api/src/repository.ts:477` (`browse`):

- Selects **every** matching title with no SQL `LIMIT`/`OFFSET`.
- Calls `relatedData()` (`repository.ts:57`) — ten more queries — for *all* of them.
- Filters visibility in JS, sorts in JS, then `items.slice(offset, offset + limit)` at the end.

So a request for 24 titles loads the entire catalog and its installments, scores, genres, tones,
tags, countries, planets, aliases, credits, and awards. At 194 titles this is survivable; it is
still ten multi-thousand-row queries per page view, and the poster/banner/logo lookups are three
correlated subqueries per row on top.

`repository.ts:574` (`titleDetail`) then issues **one episode query per installment** inside a
`Promise.all` over `data.installments` — a textbook N+1.

### 1.5 The client re-downloads the whole catalog, repeatedly

`apps/web/src/server/compat.ts:295` (`allTitles`) fetches page 1, reads `total`, then fires
`ceil(total/100) - 1` further requests **in parallel** and concatenates. `getWorks`
(`server/library.functions.ts:45`) is that function, and it is called by:

- `features/awards/awards-page.tsx:18`
- `features/awards/award-detail-page.tsx:16`
- `features/library/compare-page.tsx:63`
- `server/compat.ts:512` (planets)

`features/platform/database-page.tsx:186-210` does the same thing for the browse page via four
separate whole-catalog queries. `browseQuerySchema` (`packages/contracts/src/index.ts:205`) caps
`limit` at 100 — the client simply loops past it.

`server/compat.ts` is 713 lines whose entire job is reshaping v2 API responses back into v1 `Work`
shapes. It is the single largest piece of legacy left in the web app.

### 1.6 Nothing is virtualized, and images are half-lazy

- Zero uses of any virtualization library (`@tanstack/react-virtual` is not a dependency).
- 45 `<img>` tags; only 24 carry `loading`/`decoding` hints.
- The browse page renders every title in one grid.

### 1.7 Bundle and asset facts

From the checked-in `apps/web/dist/client` build:

| Asset | Size (uncompressed) |
| --- | --- |
| `index-*.js` (entry) | 416 KB |
| `chart-*.js` (recharts) | 336 KB |
| `styles-*.css` | 284 KB |
| `work-detail-page-*.js` | 132 KB |
| all JS | 3.1 MB across 225 chunks |
| fonts (woff2) | 472 KB, 10 files, all Noto |

Route-level code splitting **is** working. Two problems remain:

- `recharts` (336 KB) is pulled into `features/platform/work-detail-page.tsx:35` — a
  family-facing page, on the critical path of the most common journey. It is otherwise used only
  by `features/admin/pages/statistics-page.tsx`.
- ~~Three font packages are declared and never imported~~ (fixed in A5): `@fontsource-variable/alexandria`,
  `@fontsource-variable/jetbrains-mono`, `@fontsource/ibm-plex-sans-arabic`. Worse,
  `styles.css:14` sets `--font-mono: "JetBrains Mono Variable"` for a font that is never loaded,
  so every `font-mono` element silently falls back to the system monospace — which on Arabic
  content is a visible regression, and is why the code/ID chips look inconsistent.

### 1.8 TanStack is being used at about 40% of its capability

- **Zero route loaders.** Not one `loader:` or `beforeLoad:` in `apps/web/src/routes/`. All 54
  data-consuming files call `useQuery`/`useSuspenseQuery` from inside components.
- Consequence: `defaultPreload: "intent"` in `router.tsx:19` is inert — preloading runs loaders,
  and there are none. The hover-to-preload behaviour the config asks for does not happen.
- Consequence: every navigation is a waterfall — shell mounts, child mounts, *then* the request
  starts.
- `setupRouterSsrQueryIntegration` (`router.tsx:23`) is configured, but the app builds in
  `spa` mode (`vite.config.ts:58`), so its dehydrate/hydrate path never runs.
- Query keys are inconsistent: `archiveKeys`/`socialKeys`/`accountKeys` factories exist in some
  features, while `["admin-works"]`, `["entities"]`, `["works"]`, `["planets"]`,
  `["work-structure", id]` are inline string literals elsewhere. Invalidation is therefore
  guesswork.
- `/admin` has no `beforeLoad` guard (`routes/admin.tsx`); the role check happens after mount in
  `features/admin/admin-shell.tsx:110`.

### 1.9 Files over the 500-line cap

35 tracked source files exceed 500 lines; 73,327 lines of source total. Full split plan in
[Phase E](#phase-e--structure).

### 1.10 Parallel implementations of the same concepts

`features/archive/` (My Space) and `features/platform/` (Home) each own their own version of:

| Concept | Archive | Platform |
| --- | --- | --- |
| Continue watching | `archive/components/continue-watching.tsx` | `platform/components/continue-watching-rail.tsx` |
| Upcoming releases | `archive/components/upcoming.tsx` | `platform/components/upcoming-rail.tsx` |
| Family activity | `archive/components/family-activity-feed.tsx` | `platform/components/family-activity-rail.tsx` |

Both sides read the same `archiveKeys` queries and render the same data with different markup.

### 1.11 Style entropy

- 92 `!important` Tailwind overrides in TSX.
- 381 arbitrary bracket values.
- 88 hardcoded `bg-white`/`text-white`/`bg-black`/`text-black` — each one a light-mode bug waiting
  to surface once §1.1 is fixed.
- 15 raw hex colors.
- `features/platform/components/platform-shell.tsx:66-76` alone stacks `backdrop-blur-0` then
  `backdrop-blur-lg`, `sm:mx-auto!`, `p-3!`, `container!`, `sticky! top-2 z-40!`.

### 1.12 Arabic strings are not centralized

~1,296 literal Arabic strings live in TSX. `@arcadia/i18n` is imported by 6 files. The package
exists and holds taxonomy labels; it is not used as the interface vocabulary it was meant to be.

### 1.13 Android TV blockers already visible

- `src-tauri/src/player/surface.rs` embeds mpv by X11 `XID` (`gdkx11::X11Window`, `surface.rs:172`,
  `:405`), guarded by `#[cfg(target_os = "linux")]`.
- `src-tauri/src/main.rs:13` forces `GDK_BACKEND=x11`.
- `libmpv2` and `librqbit` are unconditional dependencies in `src-tauri/Cargo.toml`.
- No `LEANBACK_LAUNCHER` intent category, no `android.hardware.touchscreen required="false"`,
  no TV banner asset.

### 1.14 Housekeeping — core dumps and pins fixed in A6/A9; the crash itself is T1

- ELF core dumps keep appearing in the repo root (`core.4`, `core.27349`, and on 2026-09-13
  `core.131700` at 313 MB). They are gitignored (`core.*`) but consume working-tree space and mean
  the shell is crashing. Delete them; the crash itself is a Phase T item.
- `AGENTS.md` is a byte-identical mirror of `CLAUDE.md` except its title and one sentence. Keep it
  that way mechanically rather than editing both by hand and drifting.
- `apps/web/package.json` pins every TanStack package to `"latest"`. Builds are not reproducible
  and a `pnpm install` on another day can change router semantics under you.

### 1.15 The Tauri shell on Linux (added 2026-09-13)

The user's report — "performance, text, loading, everything is bad" — has causes on the Rust/GTK
side that the web-side findings above do not cover:

- ~~**`src-tauri/tauri.conf.json` sets `transparent: true` on the main window.**~~ Fixed in A8 — verified by hand: text is sharper and the window paints the dark `--background` natively instead of white. Nothing needs it:
  `player/surface.rs` stacks the mpv surface *above* the webview and shapes it, and `styles.css`
  paints the player body opaque black on purpose. On WebKitGTK an RGBA toplevel disables
  subpixel text antialiasing (text goes grey/soft) and forces the slower alpha compositing path
  for every frame. This is the single most likely cause of "text looks wrong".
- **`main.rs` forces `GDK_BACKEND=x11`** so mpv can embed by XID. On a scaled Wayland session
  (the dev machine runs Niri) XWayland clients render at 1× and are upscaled by the compositor —
  soft text and soft artwork on any fractional or 2× display. The root cause is the XID
  embedding model; the fix is mpv's render API (Phase T), not an env var.
- **No WebKitGTK tuning at all.** `WEBKIT_DISABLE_DMABUF_RENDERER`, `WEBKIT_FORCE_SANDBOX`, and
  the GTK/GL settings that Tauri's Linux users routinely need are neither set nor documented.
  Whether they help must be *measured* on the dev box (Phase T).
- ~~**The window has no `devtools`/perf story**~~ — fixed in T2 (`src-tauri/src/diagnostics.rs`).
- **Perceived "loading in stages"** is the web app: no route loaders (§1.8), whole-catalog
  fetches (§1.5), no virtualization (§1.6), and a 284 KB stylesheet plus 416 KB entry chunk
  (§1.7) that WebKitGTK parses on every cold start. Those are Phases G, D, and E.

---

<a id="sequencing-and-acceptance"></a>

## 2. Execution order and why

Each phase is a shippable increment. Do not start a later phase before the earlier one's
acceptance passes.

| Order | Phase | Model | Why here |
| --- | --- | --- | --- |
| 1 | **A** — correctness fixes | Opus 5 Medium · **done** | Tiny, unblocks everything, fixes a visible bug today |
| 2 | **T1/T2** — measure the shell, opaque window, devtools | Opus 5 Medium · **landed** | Gives every later phase a real number to beat |
| 3 | **E1/E2** — split the giants, delete `compat.ts` | **Sonnet 5 High** — mechanical, typecheck-guided | Every later phase edits these files; splitting after would mean doing the work twice |
| 4 | **G** — loaders + query factories | **Sonnet 5 High** | Changes the data-flow shape that D and B both build on |
| 5 | **D1/D2** — API and client pagination | Opus 5 Medium for D1a/D1b (visibility in SQL); Sonnet 5 High for D2 | Removes the whole-catalog patterns before adding a second UI mode over them |
| 6 | **C** — focus engine | Opus 5 Medium | The centerpiece; needs G's preload-on-focus and D's fast routes to feel right |
| 7 | **B** — Display Mode | Opus 5 Medium for B1/B4; Sonnet 5 High for applying `tv:` variants | Consumes C's tokens and E3's structure |
| 8 | **F** — UI unification | **Sonnet 5 High** (F1 inventory: Sonnet 5 Medium) | Best done once both modes exist, so the shared vocabulary is real |
| 9 | **D3/D4** — rendering and bundle | Sonnet 5 High | Measure against the finished thing |
| 10 | **T3/T4** — native Wayland, packaging | Opus 5 Medium | Needs a stable web app to measure against |
| 11 | **H** — Android boundaries | Opus 5 Medium | Independent; can run in parallel with 7–10 |

Each phase's first step is to read `AGENT-CONTEXT.md`; its last step is to update it (moved
files, new commands, new gotchas) and tick the boxes above.

---

<a id="phase-a--correctness-fixes"></a>

## Phase A — Correctness fixes that block everything else · ✅ Done (2026-09-13)

> Small, independent, and each one unblocks later phases. Do these first, in one PR.

- [x] **A1. Fix light mode.** Remove `className="dark"` from `<body>` in
      `routes/__root.tsx:70`. The inline restore script already sets `html.dark` and
      `html.style.colorScheme` before first paint, which is the correct single source of truth.
- [x] **A2. Add a `system` theme option.** `preferences.theme` becomes
      `"dark" | "light" | "system"`; `system` reads `matchMedia("(prefers-color-scheme: dark)")`
      and subscribes to changes. Requires a `packages/contracts` change and
      `pnpm client:generate`.
- [x] **A3. Extract a `useTheme` hook** (`apps/web/src/lib/theme.ts`) that owns the localStorage
      key, the `html` class, `colorScheme`, and the media-query subscription. `settings-page.tsx`
      and the restore script both go through it. Nothing else touches `arcadia:theme` directly.
- [x] **A4. Audit the 88 hardcoded black/white utilities** against light mode. Replace with
      semantic tokens except where the comment at `work-card.tsx:180-183` applies (chips sitting
      on artwork, which are intentionally theme-independent) — keep those and mark them with a
      `data-on-artwork` attribute so the intent is greppable.
- [x] **A5. Load the mono font or drop it.** Either add
      `@import "@fontsource-variable/jetbrains-mono"` to `styles.css`, or change `--font-mono` to a
      system stack. Then remove `@fontsource-variable/alexandria` and
      `@fontsource/ibm-plex-sans-arabic` from `apps/web/package.json`.
- [x] **A6. Delete the core dumps** (`rm core.4 core.27349`) and note in `README.md` that the
      Tauri shell drops them next to the launch directory.
- [x] **A7. Add a `beforeLoad` role guard to `routes/admin.tsx`** that redirects non-owner/editor
      sessions to `/`, so the admin bundle is never even requested by a family profile.
- [x] **A8. Make the Tauri window opaque.** `transparent: false` in `src-tauri/tauri.conf.json`
      (see §1.15). Verify by running `devenv shell -- pnpm tauri dev`, opening the player, and
      confirming the video surface and the shaped control cut-outs still work — the design in
      `surface.rs` does not depend on alpha, but this is the one change in Phase A that needs the
      real shell to confirm.
- [x] **A9. Pin the `"latest"` TanStack ranges** in `apps/web/package.json` to the versions the
      lockfile already resolved, so `pnpm install` is reproducible.

**Acceptance:** toggling فاتح/داكن/تلقائي in settings visibly changes the app, survives reload,
and no page has an unreadable region in light mode. `pnpm check` and `pnpm build` pass.

**Status (2026-09-13):** all nine items landed in `eabd81d`; A8 verified by hand in the shell
(the window also gets `backgroundColor: "#070a0f"`, the dark `--background`, and the restore
script paints `<html>` the same before CSS loads — no white flash). Notes:

- `pnpm check` was red on the baseline (481 oxlint + 6 biome errors); that debt was cleared the
  same day — see §1.0 — so `pnpm check` is now a real gate.
- `pnpm client:generate` was not needed for A2: `/api/v1/me` is not in the OpenAPI document, so
  the web app reads `SessionAccount` straight from the Zod types. `account_preferences.theme` is
  a `text` column — no migration.
- A4 audit rules as applied: page chrome (`border-white/8`, `bg-white/5`, `hover:bg-white/6`,
  `ring-white/10`, planet-card chips) became `border-border`, `bg-muted/40`, `hover:bg-accent`,
  `ring-foreground/10`, `bg-background/60` + `text-foreground/80`. Elements over posters,
  banners, logos, and the video plane keep black/white and carry `data-on-artwork` (on the
  element or its nearest container — 30 tags). `shadow-black/N` is left alone everywhere:
  shadows are black in both themes. Vendored `components/ui/*` scrims are exempt.
- The role guard needed `queryClient` in the router context
  (`createRootRouteWithContext`) and a `currentAccountQueryOptions()` factory — the first
  Phase G checkbox is therefore already done.
- Still owed: a page-by-page light-mode pass (folds into Phase F's inventory).

---

<a id="phase-t--tauri-shell"></a>

## Phase T — The Tauri shell on Linux · 🟡 T1/T2 landed (2026-09-13)

> Goal: the desktop build feels native on NixOS/Fedora — crisp text at any scale, no crash dumps,
> a cold start that paints the shell within a second on the dev laptop. Everything here is Rust,
> GTK, or configuration; the web-side work that also affects "feel" lives in Phases D, E, and G.
> **Model:** Opus 5 Medium — GTK/X11/mpv interactions are subtle and expensive to get wrong.

### T1. Measure before changing (one session; write the numbers into `AGENT-CONTEXT.md`)

- [x] Instrumented: with `ARCADIA_LOG=info` the shell logs `setup`, `page load started`, and
      `page loaded` as `+<ms>` since process start.
- [ ] **Read the numbers** from three release-build runs and record them in
      `AGENT-CONTEXT.md` §6 (needs a human at the machine).
- [ ] `WEBKIT_DISABLE_DMABUF_RENDERER=1` on/off; `GDK_SCALE`/`GDK_DPI_SCALE` under XWayland; note
      what each does to text sharpness and scroll frame time. Record, do not guess.
- [ ] Reproduce the crash behind the `core.*` dumps: the next time one appears, `gdb -c core.NNN`
      before deleting it; note the faulting library (WebKit, mpv, GTK) and the trigger. (The
      2026-09-13 dump was deleted before analysis.)

### T2. Window and process

- [x] `transparent: false` (A8) plus native `backgroundColor` = dark `--background`.
- [x] `--devtools` / `ARCADIA_DEVTOOLS=1` opens the WebKit inspector in release builds
      (`src-tauri/src/diagnostics.rs`; the `devtools` cargo feature is on).
- [x] Window geometry stays in `tauri.conf.json` (1280×800, min 960×600, decorated) — decided,
      nothing to move.
- [x] `tauri_plugin_log` is always registered; `ARCADIA_LOG` sets the level (release default
      `warn`).

### T3. Native Wayland (the real fix for text)

The XID embedding model is what forces XWayland. Replace it with mpv's **render API** drawing
into a `gtk::GLArea`, *if* the Tauri widget-tree constraint documented at the top of
`surface.rs` can be honoured (the GLArea must not become the webview's ancestor). Options, in
order of preference:

1. A **sibling `GLArea` under the webview** with the webview made transparent *only on the player
   route* via `set_background_color` — revisits the "video below a transparent webview" case that
   X11 cannot blend but Wayland can.
2. **`mpv` in a separate top-level, always-on-top window** positioned to track the main window —
   the fallback Wayland actually supports; loses the shaped-cut-out elegance.
3. Keep XID embedding; accept XWayland; document the scaling limitation. Current state.

Decide with T1's numbers and a prototype of option 1, not by preference.

### T4. Packaging

- [ ] Confirm the AppImage/deb/rpm from CI runs on a stock Fedora with only the documented
      runtime packages. Add the missing ones to `bundle.linux.*.depends`.
- [ ] Keep `librqbit`/`libmpv2` under target-specific tables (H2b) so a mobile build does not pull
      them.

**Acceptance:** text is crisp at 1×, 1.5×, and 2× scale; no `core.*` files after a day of use;
cold start to interactive under 1.5 s on the dev laptop (release build). Numbers recorded in
`AGENT-CONTEXT.md`.

---

<a id="phase-e--structure"></a>

## Phase E — File-size and structure cleanup

> **Hard rule from this release forward: no source file over 500 lines.** Add an oxlint rule (or a
> `tools/` script wired into `pnpm check`) that fails the build, so the cap is enforced rather than
> remembered. Generated files (`routeTree.gen.ts`, `contracts/src/generated.ts`) and `*.test.ts`
> are exempt; `packages/database/src/schema.ts` gets a documented exemption or is split by domain.

**Per-file acceptance (added 2026-09-14):** every file produced by a split or a deletion below
is removed from the Phase E allowlist in `oxlint.config.ts` in the same commit and lints clean
with no override. The allowlist reaching zero entries is E's exit condition.

### E1. Split plan

| File | Lines | Split into |
| --- | --- | --- |
| `web/features/platform/work-detail-page.tsx` | 2454 | `work-detail/` — `route.tsx`, `hero.tsx`, `overview-tab.tsx`, `installments-tab.tsx`, `people-tab.tsx`, `relations-tab.tsx`, `scores.tsx` (lazy), `awards.tsx`, `use-work-detail.ts` |
| `web/features/admin/components/editor-form/index.tsx` | 2434 | one file per tab under `editor-form/tabs/`; `index.tsx` keeps only composition + form state |
| `api/src/app.ts` | 2112 | `features/titles/routes.ts`, `features/streams/routes.ts`, `features/subtitles/routes.ts`, `features/admin/{catalog,media,vocabularies,entities,statistics,validation}/routes.ts`; `app.ts` keeps middleware + mounts only (target < 200 lines) |
| `database/src/schema.ts` | 1530 | `schema/{titles,installments,episodes,people,studios,planets,accounts,social,awards,media,audit}.ts` re-exported from `schema/index.ts` — **or** an explicit exemption, decided before starting |
| `web/features/admin/pages/awards-management-page.tsx` | 1319 | `admin/awards/{organizations,categories,ceremonies,recognitions}-panel.tsx` + a shell |
| ~~`web/features/library/player-controls.tsx`~~ | 1198 | **done 2026-09-18** → `player/components/{control-bar,timeline,volume-control,top-bar,mobile-transport,status-overlays,center-feedback,control-button}.tsx` + `player/panels/{panel-shell,tracks-panel,subtitles-tab,language-group-list,speed-panel,source-panel,episodes-panel,more-panel}.tsx` |
| `cli/src/commands/work.ts` | 1193 | `work/{list,get,apply,validate}.ts` |
| `web/features/admin/json-editor/engine.ts` | 1168 | `json-editor/engine/{parse,validate,diff,apply}.ts` |
| `web/features/admin/json-editor/json-editor-page.tsx` | 1149 | page shell + `panels/` |
| `contracts/src/index.ts` | 1137 | `schemas/{title,installment,episode,account,admin,awards,social,archive,media}.ts` re-exported |
| `web/features/admin/pages/entities-management-page.tsx` | 1004 | list / editor / merge panels |
| `contracts/src/admin-field-registry.ts` | 1002 | per-entity registry files |
| `web/features/admin/pages/statistics-page.tsx` | 938 | one file per chart group; charts lazy |
| ~~`web/features/library/player-page.tsx`~~ | 937 | **done 2026-09-18** → `player/player-page.tsx` (283) + `player/hooks/{use-player-session,use-player-actions,use-player-shortcuts,use-overlay-regions,use-controls-visibility,use-progress-persistence,use-playhead-paint,use-feedback}.ts` |
| `web/features/social/title-social-section.tsx` | 872 | reviews / comments / ratings panels |
| `api/features/social/routes.ts` | 808 | reviews / comments / playback route modules |
| `web/features/platform/studio-lineage-page.tsx` | 806 | page + graph layout module + node components |
| `web/features/admin/pages/profiles-page.tsx` | 771 | list / policy editor / capability editor |
| `web/src/server/compat.ts` | 713 | **deleted** — see E2 |
| `web/features/library/compare-page.tsx` | 692 | picker / matrix / detail column |
| `web/features/platform/database-page.tsx` | 690 | filters / results / view-mode switch |
| `api/src/repository.ts` | 675 | `repository/{browse,detail,visibility,mappers}.ts` |
| `web/features/library/model.ts` | 666 | split by entity once `compat.ts` is gone; much of it dies with it |
| `web/features/profiles/settings-page.tsx` | 658 | one file per tab: `settings/{profile,safety,notifications,appearance,display-mode}.tsx` |
| `web/features/admin/pages/media-library-page.tsx` | 645 | grid / uploader / assignment panel |
| `api/features/archive/routes.ts` | 603 | library / history / notifications / recommendations |
| `web/features/catalog/catalog-filters.tsx` | 590 | filter groups + shared chip primitives |
| `api/features/accounts/routes.ts` | 574 | session / profile / invites / capabilities |
| `web/features/library/offline-title-page.tsx` | 552 | page + transfer panel |
| `api/integrations/torrent-source.ts` | 535 | client / candidate ranking / fixtures |
| `web/features/profiles/profiles-page.tsx` | 529 | picker + editor |
| `web/features/admin/pages/planets-management-page.tsx` | 510 | list + editor |
| `web/src/server/library.functions.ts` | 506 | fold into per-feature `api.ts` modules (see E3) |
| `web/components/ui/sidebar.tsx` | 691 | vendored shadcn — **exempt**, do not hand-edit |
| `tools/oxlint/anti-slop/shared/dictionary-types.ts` | 502 | data file — exempt |

### E2. Delete the v1 compatibility layer

`server/compat.ts` (713 lines) plus the `Work`/`WorkStructure` half of
`features/library/model.ts` (666 lines) exist only to translate v2 API shapes back into v1 names.
The v2 contracts are the real types. This is ~1,000 lines of pure translation that can be deleted
by migrating consumers to `@arcadia/contracts` types directly. Do it feature by feature, not in one
commit, and let `pnpm typecheck` be the guide.

### E3. Target structure for `apps/web/src`

```
src/
  app/            router, providers, root shell, mode + theme + focus providers
  components/
    ui/           shadcn primitives (vendored, never hand-edited)
    patterns/     app-level compositions: PageHeader, EmptyState, Rail, StatTile, ...
  features/<name>/
    api.ts        query keys + query/mutation options factories  (one per feature, mandatory)
    model.ts      feature types derived from @arcadia/contracts
    components/   feature-local components
    pages/        route-level components
    <name>.test.ts
  lib/            theme, app-mode, focus/, utils, api client
  routes/         thin file-based route entries: loader + component import only
```

Rules that follow from it:

- A route file does route things: `validateSearch`, `beforeLoad`, `loader`, and a component
  import. No JSX bodies beyond a one-line render.
- `features/*/api.ts` is the only place a query key is written.
- Cross-feature imports go through the feature's public surface, never into
  `features/x/components/y`.
- `server/` disappears; its contents move into the owning feature's `api.ts`. The name has been
  misleading since day one — `CLAUDE.md` already has to explain that they are not server functions.

### E4. Merge the duplicated Archive/Platform components

Collapse the three pairs in §1.10 into `components/patterns/`:
`ContinueWatchingRail`, `UpcomingRail`, `FamilyActivityRail`, each taking a `variant` prop for the
two layouts that genuinely differ. One data hook per concept in `features/archive/api.ts`.

---

<a id="phase-g--tanstack"></a>

## Phase G — TanStack modernization

### G1. Loaders everywhere

The single highest-leverage change in this phase. Every route that shows data gets a `loader` that
calls `queryClient.ensureQueryData(...)`:

```ts
// routes/titles.$titleId.tsx
export const Route = createFileRoute("/titles/$titleId")({
  loader: ({ context, params }) =>
    context.queryClient.ensureQueryData(titleQueryOptions(params.titleId)),
  component: WorkDetailRoute,
});
```

This makes `defaultPreload: "intent"` (`router.tsx:19`) real: hovering or D-pad-focusing a card
starts the fetch, so by the time Enter is pressed the detail page has data. On a TV, where the
selection lingers on a card for a second or two before activation, **this alone removes most
perceived latency**. Raise `defaultPreloadStaleTime` from `0` so preloads are not immediately
discarded.

- [ ] Add `queryClient` to the router context (`createTanStackRouter({ context: { queryClient } })`).
- [ ] `pendingComponent` per route, using the shared skeleton patterns from F1.
- [ ] `errorComponent` per route; the root one at `__root.tsx:31` stays as the backstop.
- [ ] Preload on **focus**, not just pointer intent — a `preload` call from the focus provider when
      a card is selected for > 250 ms.

### G2. Query options factories

One `features/<name>/api.ts` per feature, exporting `queryOptions()` objects:

```ts
export const titleKeys = {
  all: ["titles"] as const,
  list: (filters: BrowseFilters) => [...titleKeys.all, "list", filters] as const,
  detail: (id: string) => [...titleKeys.all, "detail", id] as const,
};

export const titleQueryOptions = (id: string) =>
  queryOptions({ queryKey: titleKeys.detail(id), queryFn: () => getTitle(id) });
```

Then delete every inline `queryKey: ["works"]`. Invalidation becomes `titleKeys.all` and is
correct by construction.

### G3. Search params as state

`/browse` currently keeps filters in component state and only `q` in the URL
(`routes/browse.tsx`). Move the full filter set into `validateSearch` with a Zod schema, and read
it in the loader. This gives back/forward, shareable filtered views, and — the reason it matters
for TV — a **restorable position**, which is the "Returning means returning" rule that
`v0.3-roadmap.md` set and never fully delivered.

### G4. Mutations

- [ ] Standardize on `useMutation` with `onSettled` invalidation against the key factories.
- [ ] Optimistic updates for the cheap toggles that must feel instant on a remote: favorite,
      watched, save-offline.
- [ ] One `sonner` toast convention for success and failure; delete the per-page alert blocks.

### G5. Decide the SSR-query question

`setupRouterSsrQueryIntegration` (`router.tsx:23`) is dead weight under `spa` mode. Either remove
it, or — the better option if the web build ever gets its own deployment separate from Tauri —
document precisely when it applies. Do not leave it as unexplained ceremony.

---

<a id="phase-d--performance"></a>

## Phase D — Performance

> Budget, stated up front, measured on the weakest target: **home route interactive < 2.0 s cold on
> an Android TV box; selection moves within 100 ms of a key press, always; no frame over 32 ms
> while moving through a rail.**

### D1. API — page in SQL

- [ ] **D1a. Push `LIMIT`/`OFFSET` into `browse()`** (`repository.ts:477`). Visibility filtering
      moves into the SQL predicate using the account's policy, so pagination and filtering happen
      in one place. `total` comes from a `count(*) over ()` window, not `items.length`.
- [ ] **D1b. Materialize primary artwork.** The three correlated subqueries per row
      (`repository.ts:492-495`, repeated verbatim in four places) become one
      `title_primary_media` view — or, if `EXPLAIN` justifies it, a small denormalized column
      updated by `purgeUnreferencedMedia`'s existing write path.
- [ ] **D1c. Kill the `titleDetail` N+1** (`repository.ts:574`). One `where installment_id in (...)`
      query, grouped in JS.
- [ ] **D1d. Index audit.** Confirm indexes exist for every hot filter: `titles(sort_title)`,
      `titles(is_private)`, `installments(title_id, position)`, `episodes(installment_id, position)`,
      `media_asset_assignments(title_id, role, is_primary)`, `contributions(title_id)`, and the
      `title_*` join tables' `title_id`. 100 index declarations exist across 65 tables; verify the
      right ones rather than adding blindly.
- [ ] **D1e. Trigram index for search.** `pg_trgm` + GIN on `titles.canonical_title`,
      `titles.title_ar`, `title_aliases.title` — the current `ilike '%…%'` in
      `repository.ts:481` cannot use a btree index at all.
- [ ] **D1f. Add `Cache-Control` and `ETag`** to `GET /api/v1/titles` and
      `/api/v1/titles/{titleId}`. A family archive changes rarely; a 304 on a TV box is free.

### D2. Client — stop downloading the catalog

- [ ] **D2a. Delete `allTitles`' pagination loop** (`compat.ts:295`). Every caller becomes a
      properly paginated or filtered query.
- [ ] **D2b. Awards pages** (`awards-page.tsx`, `award-detail-page.tsx`) need award→title joins,
      not the catalog. Add `GET /api/v1/awards/…?expand=titles` returning the summaries the page
      actually shows.
- [ ] **D2c. `/browse`** (`database-page.tsx:186-210`) moves to server-side pagination with search
      params as the source of truth (see [Phase G](#phase-g--tanstack)), plus
      `useInfiniteQuery`.
- [ ] **D2d. `/compare`** keeps whole-catalog access but behind an explicit picker — it loads when
      you open the picker, not when you open the route.
- [ ] **D2e. Planets** (`compat.ts:512`) gets a real endpoint returning planet + counts, with
      titles fetched per planet on demand.

### D3. Rendering

- [ ] **D3a. Virtualize.** Add `@tanstack/react-virtual`. Apply to the browse grid, admin tables,
      the media library, and any rail over ~30 items. This is non-negotiable for the TV target.
- [ ] **D3b. Every `<img>` gets `loading`, `decoding="async"`, explicit `width`/`height`, and an
      `aspect-ratio` box.** Above-the-fold hero art gets `fetchpriority="high"` and `loading="eager"`;
      everything else is lazy. 21 images currently have none of this.
- [ ] **D3c. Serve responsive artwork.** `media-storage.ts` is already content-addressed; add
      derivative widths at ingest (`-w320`, `-w640`, `-w1280`) and emit `srcset`. A TV grid pulling
      full-resolution posters is the largest single byte cost on the home route.
- [ ] **D3d. Drop `backdrop-blur` in Display Mode.** `platform-shell.tsx:66-76` uses it on a
      sticky header — a full-viewport blur recomputed on every scroll frame. Replace with a solid
      or gradient surface under the `tv` variant.
- [ ] **D3e. Lazy-load recharts.** `React.lazy` the score-visualization block in
      `work-detail-page.tsx`, and render a token-styled bar list in Display Mode instead. Removes
      336 KB from the most-visited route.
- [ ] **D3f. `content-visibility: auto`** on off-screen rail sections, with
      `contain-intrinsic-size` to keep the scrollbar honest.
- [ ] **D3g. Replace the 120 ms `setInterval` measure loop** in `player/hooks/use-overlay-regions.ts` with a
      `ResizeObserver`. The rAF playhead and 4 Hz Rust tick in that file are already correct and
      should be left alone.

### D4. Bundle

- [ ] Route-based splitting already works — keep it and add a size budget check to CI.
- [ ] Audit `@phosphor-icons/react` imports; confirm the per-icon chunks visible in `dist` are not
      duplicating a barrel import somewhere.
- [ ] Move `shadcn` to `devDependencies` if `styles.css`'s `@import "shadcn/tailwind.css"` resolves
      from there (verify — do not assume).
- [ ] Tailwind CSS at 284 KB deserves one look at whether unused shadcn base layers are shipping.

---

<a id="phase-c--focus-engine"></a>

## Phase C — The focus engine rebuild

> Target: Steam Big Picture. The specific qualities worth copying are **(a)** the selected item has
> a *filled* surface, not an outline; **(b)** there is exactly one selected thing on screen, always;
> **(c)** the selection moves with a short, consistent, interruptible animation; **(d)** the
> selection is never ambiguous during a transition — it moves, it does not fade out and in
> somewhere else.

### C1. The selection token set

Add to `styles.css`, alongside the existing semantic tokens:

```css
:root {
  --selection-surface: var(--primary);
  --selection-foreground: var(--primary-foreground);
  --selection-ring: oklch(1 0 0 / 0.9);
  --selection-glow: 0 0 0 2px var(--selection-ring), 0 12px 40px -8px var(--primary);
  --selection-scale: 1.06;
  --selection-duration: 180ms;
  --selection-ease: cubic-bezier(0.16, 1, 0.3, 1);
}
```

Two variants, one rule:

- **Chrome selection** (tabs, nav items, buttons, list rows, settings controls): the element's
  background becomes `--selection-surface` and its text `--selection-foreground`. Fully filled.
  This is the "white/primary background" the user described, and it is the one that reads across a
  room.
- **Artwork selection** (poster cards, banner cards): the artwork cannot be tinted, so the card
  gets `--selection-glow` plus `scale(var(--selection-scale))` plus a full-opacity title, while
  every *unselected* sibling in the same rail drops to `opacity: 0.55`. The dimming is what makes
  it unambiguous — Big Picture and the Android TV leanback guidance both do this.

Every focus treatment in the app resolves to one of those two. The four competing treatments in
§1.3 collapse to this pair.

### C2. Replace the DOM scanner with a declarative registry

Delete `AutomaticSpatialTargets` (`spatial-navigation.tsx:140-244`). Replace with:

```
apps/web/src/lib/focus/
  ├─ focus-provider.tsx     one context; owns current focus id + the section graph
  ├─ use-focusable.ts       registers an element; returns { ref, focused, selected }
  ├─ use-focus-section.ts   declares a group (rail / grid / column) + its geometry policy
  ├─ focus-geometry.ts      pure: given a rect, a direction, and candidate rects → next id
  ├─ focus-geometry.test.ts pure unit tests, no DOM
  └─ focusable.tsx          <Focusable> render-prop wrapper for the leaf case
```

Design rules:

1. **Registration is explicit and React-owned.** A component that can be focused says so. No
   `querySelectorAll`, no `MutationObserver`, no attribute sniffing. Registration/unregistration
   happens in `useLayoutEffect`, which is exactly when the component actually exists.
2. **Geometry is cached per section, invalidated on resize and on section content change** — not
   read fresh per keypress. `getClientRects()` never runs inside a keydown handler.
3. **Sections are a graph, not a plane.** A rail declares `onUp`/`onDown` to named sibling
   sections. This is what fixes the `resolveTabPanelEscape` special case
   (`spatial-navigation.tsx:95-112`) — that whole function becomes an ordinary section edge.
4. **Focus memory is per section, per route.** Leaving a rail and returning puts you back on the
   card you left, which is the single biggest "feels native" affordance. The existing
   `rememberedFocusByLocation` map (`spatial-navigation.tsx:22`) has the right idea and the wrong
   granularity — it remembers one thing per URL, keyed on a screen-space centroid.
5. **One selected element, always.** The provider guarantees it. If the currently-focused element
   unmounts, the provider selects the nearest surviving sibling in the same section synchronously,
   not on the next animation frame.
6. **Key handling is one listener on the provider**, not one per element. Arrow, Enter, Escape,
   Back, and the Android TV media keys are translated to intents there.
7. **Scrolling is the section's job.** A rail scrolls itself so the selected card sits at a
   consistent inline offset. `revealSpatialTarget` (`spatial-navigation.tsx:63`) becomes a section
   method with the rail's own geometry, instead of a global function guessing at safe areas from
   `window.innerHeight * 0.18`.

### C3. Whether to keep `@noriginmedia/norigin-spatial-navigation`

**Recommendation: keep the core, drop the React wrapper, own the integration.**

The library's geometry engine is sound and battle-tested; the pain in this codebase comes entirely
from the adapter layer we wrote around it. So: keep
`@noriginmedia/norigin-spatial-navigation-core` as the geometry primitive behind
`focus-geometry.ts`, and drop `@noriginmedia/norigin-spatial-navigation-react` in favour of the
provider above. If `focus-geometry.test.ts` ends up proving the pure implementation is adequate,
dropping the core too becomes a trivial follow-up rather than a rewrite.

Evaluate `@bbc/tv-lrud-spatial` as the alternative during C2 — it is smaller, DOM-attribute-driven,
and used in production on real TV hardware. Decide with a benchmark, not a preference.

### C4. Everything is focusable, and obviously so

The user's "really everything" requirement, enumerated so it can be checked off:

- [ ] Nav items and the profile menu
- [ ] Every card in every rail, and every card in every grid
- [ ] Tabs and tab panels
- [ ] All buttons, including the card hover-tray actions in `work-card-actions.tsx`
- [ ] Selects, switches, checkboxes, sliders (volume, subtitle offset)
- [ ] Text inputs — with an explicit **enter-to-edit / Back-to-exit** model, so arrows do not get
      swallowed by a search box the way `isTextEntryTarget` currently has to work around
      (`spatial-navigation.tsx:47`)
- [ ] Dialog, sheet, drawer, and dropdown contents, with focus trapped inside and Back closing
- [x] Player controls — `player/` D-pad model: bar rows (`data-control-row`) + `PanelShell` walk; Enter clicked explicitly because the engine cancels native activation
- [ ] Pagination, filter chips, sort controls
- [ ] Empty-state and error-state actions
- [ ] The on-screen keyboard in Display Mode search

### C5. A visible focus contract

Add a dev-only overlay (`?focus-debug=1`) that draws the section graph and the current selection
path. This is how the "geometric jumps still feel imperfect" note carried forward from
`v0.2-roadmap.md` gets closed with evidence instead of impressions.

---

<a id="phase-b--two-modes"></a>

## Phase B — Two modes: Normal and Display

> **Answer to "can we do that?": yes, and it is the right shape for this app.** The important
> design decision is *what a mode actually is*, because the wrong answer means maintaining two
> apps.

### B1. What a mode is (and is not)

A mode is **not** a second route tree, a second component library, or a fork of the pages. It is:

1. A **layout density and interaction contract** applied at the shell level.
2. A **route visibility filter** (Display Mode hides, never deletes, admin and desk-only surfaces).
3. A **capability flag** that individual components may read to swap a heavy affordance for a
   TV-appropriate one.

Concretely: one `AppModeProvider` near the root, one `data-mode="normal" | "display"` attribute on
`<html>`, and Tailwind variants keyed off it. Pages stay single-source; a rail is the same rail,
it is just bigger, has no hover state, and has a different focus treatment.

```
apps/web/src/lib/app-mode.tsx
  ├─ AppModeProvider           reads preference, writes html[data-mode], exposes context
  ├─ useAppMode()              → { mode, isDisplay, setMode }
  └─ <DisplayOnly> / <NormalOnly>   tiny render guards for the handful of real divergences
```

```css
/* styles.css */
@custom-variant tv (&:is([data-mode="display"] *));
@custom-variant desk (&:is([data-mode="normal"] *));
```

Usage stays declarative and reviewable: `class="text-sm tv:text-lg"`, `class="gap-3 tv:gap-6"`.

### B2. What Display Mode changes

| Area | Normal | Display |
| --- | --- | --- |
| Base type scale | 14–16 px | 18–22 px (`html { font-size }` bump via the `tv` variant) |
| Minimum hit target | 32 px | 56 px |
| Focus treatment | ring on an unchanged surface | **filled selection** — see [Phase C](#phase-c--focus-engine) |
| Hover states | full | **none** — `tv:hover:*` is stripped; hover on TV means nothing and causes flicker |
| Admin routes | visible | **hidden from nav and blocked by `beforeLoad`**, not deleted |
| Global search | inline combobox (`cmdk`) | full-screen overlay with an on-screen keyboard |
| Dialogs / dropdowns | popovers anchored to trigger | full-width bottom sheets or full-screen panels |
| Tables (`work-table.tsx`, admin grids) | rendered | replaced by card rails |
| Charts (recharts) | rendered | **not rendered** — the score radar becomes a static bar list |
| Animations | full | reduced: opacity + transform only, no blur/shadow transitions |
| Backdrop blur | yes | **no** — `backdrop-filter` is the single most expensive thing a TV WebView does |
| Density | up to 8 cards/row | 5–6 cards/row, larger posters |
| Scroll | free | rail-snapped, focus-driven only |

### B3. Which routes exist in Display Mode

| Route | Display Mode |
| --- | --- |
| `/` (home) | ✅ primary surface |
| `/browse` | ✅ redesigned as a rail/grid browser, not a table |
| `/titles/$titleId` | ✅ |
| `/player/$installmentId` | ✅ already D-pad-native |
| `/archive` (My Space) | ✅ |
| `/awards`, `/_entities/*` | ✅ simplified |
| `/profiles`, `/settings` | ✅ simplified — settings is where you leave Display Mode |
| `/compare` | ❌ hidden (desk-only analysis tool) |
| `/admin/*` | ❌ hidden from nav, `beforeLoad` redirects to `/` |
| `/offline` | ✅ |

Hiding is done in two places and both are required: the nav model (so the link is absent) and
`beforeLoad` (so a remembered URL or a deep link cannot land you there).

### B4. Persistence and entry

- [ ] Add `preferences.displayMode: "auto" | "normal" | "display"` to `accountPreferencesSchema`
      in `packages/contracts/src/index.ts`, migrate, and `pnpm client:generate`.
- [ ] **Per-account, server-persisted** — that is the whole point of putting it in settings, and it
      means the TV in the living room and the laptop can hold different postures for the same
      family member only if we also… (see next item).
- [ ] **Per-device override in `localStorage`** (`arcadia:display-mode`), which wins over the
      account preference when set. The TV box sets it once; the laptop never does.
- [ ] `"auto"` resolves to Display when **any** of: `matchMedia("(pointer: coarse)")` **and** a
      viewport ≥ 1280 px wide, Tauri reports an Android target, or the URL carries `?tv=1`.
      Practically: real TVs land in Display; laptops do not.
- [ ] Settings gets an **العرض / وضع التلفاز** section as its own tab, above المظهر.
- [ ] A keyboard/remote shortcut toggles it from anywhere (long-press the menu key, or `Ctrl+Shift+T`
      on desktop) with a confirmation toast, so a family member stuck in the wrong posture can get
      out without navigating a UI built for the other one.

### B5. Should the D-pad be removed from Normal Mode?

**No — but it should stop being a bespoke engine there.** Recommendation:

- In **Normal Mode**, delete `AutomaticSpatialTargets` entirely and rely on the browser's own
  sequential focus (`Tab`) plus roving-tabindex inside rails and grids. That is what shadcn/Base UI
  primitives already implement, it is what screen readers expect, and it removes the
  document-wide MutationObserver from the desk experience completely.
- In **Display Mode**, the focus engine from [Phase C](#phase-c--focus-engine) is the only
  navigation model.

This is the cleanup the user asked about: not "remove D-pad", but "stop running a TV navigation
engine on top of a desktop app". Arrow keys still move within rails and grids in Normal Mode,
because that is standard widget behaviour, not a spatial engine.

---

<a id="phase-f--uiux-unification"></a>

## Phase F — UI/UX unification on shadcn

### F1. A component inventory pass

Before writing anything: list every visual pattern that appears on more than one page (page
header, section header, stat tile, empty state, error state, loading skeleton, classification
badge, score display, person chip, filter chip, action tray, pagination). For each, pick **one**
implementation, put it in `components/patterns/`, and delete the rest. `RailHeader`
(`rail.tsx:81`) and `admin-page-header.tsx` are two solutions to the same problem on opposite
sides of the app.

### F2. Missing shadcn primitives worth adding

Checked against `components/ui/`, these are being hand-rolled today and should be installed via
the `shadcn` skill rather than written:

- `sonner` — there is no toast system at all; mutation feedback is ad-hoc per page.
- `pagination` — the browse page needs it once D2c lands.
- `hover-card` — for person/studio previews currently done with bespoke popovers.
- `scroll-area` — rails hand-roll `scrollbar-none` and mask gradients.
- `resizable` — admin split views.
- `sidebar` is installed but only used in admin; the Display Mode nav may want it.

### F3. Style discipline

- [ ] Eliminate the 92 `!important` overrides. Each one means a component variant is missing —
      add the variant to the primitive instead. `platform-shell.tsx` alone accounts for a large
      share.
- [ ] Reduce the 381 arbitrary bracket values to a documented set of tokens. Sizes like
      `auto-cols-[43%]` (`rail.tsx:114`) are legitimately a design ladder — move them into
      `@theme` as named values (`--rail-col-poster-sm`, etc.) so they are greppable and tunable
      per mode.
- [ ] Zero raw hex; zero non-token `black`/`white` outside the `data-on-artwork` exemption.
- [ ] One motion vocabulary: `--selection-duration`/`--selection-ease` from C1, plus a
      `--layer-duration` for dialogs. Nothing else defines its own timing.

### F4. Arabic strings

Move interface vocabulary out of TSX into `@arcadia/i18n`. Do **not** attempt full i18n
infrastructure — the app is Arabic-first by design and adding a second locale is not a v3.2 goal.
The goal is consistency: "مساحتي" should be one constant, not seventeen literals, so a wording
change is one edit. Start with the ~200 strings that appear more than once; leave genuinely
one-off page copy inline.

---

<a id="phase-h--android-tv"></a>

## Phase H — Android TV readiness

> Not shipped in v3.2. The goal for this release is that **nothing in the web app blocks it**, and
> that the Rust boundaries are drawn in the right place.

### H1. What already blocks it

- `src-tauri/src/player/surface.rs` embeds mpv via X11 `XID` (`gdkx11`), `#[cfg(target_os = "linux")]`.
- `src-tauri/src/main.rs:13` forces `GDK_BACKEND=x11` and strips `WAYLAND_DISPLAY`.
- `libmpv2` and `librqbit` are unconditional in `Cargo.toml`.

### H2. Work for this release

- [ ] **H2a. Define a `PlaybackBackend` trait** in `src-tauri/src/player/` with the current mpv/X11
      implementation behind `#[cfg(target_os = "linux")]`, so an Android implementation is an added
      file rather than a refactor. The IPC surface the web app talks to (`player_subscribe`, the
      4 Hz `Channel<PlayerEvent>` tick) stays identical — that contract is already good and should
      not change.
- [ ] **H2b. Move `libmpv2` and `librqbit` under target-specific dependency tables.**
- [ ] **H2c. Add the Android manifest additions** (`LEANBACK_LAUNCHER` intent category,
      `android.hardware.touchscreen` not required, `android.software.leanback` feature, TV banner)
      to `src-tauri/gen/android/` when it is generated, and document them in `README.md`. Without
      `LEANBACK_LAUNCHER` the app does not appear on the Android TV launcher at all.
- [ ] **H2d. Key mapping.** Android TV remotes emit `KEYCODE_DPAD_*` and `KEYCODE_BACK`; the System
      WebView surfaces most as standard `ArrowUp`/`Enter`/etc. key events, but media keys and Back
      need explicit handling. The Phase C provider is the single place that translates them —
      write it with a `KeyMap` indirection from day one rather than hardcoding `event.key`.
- [ ] **H2e. Measure on real hardware once.** Even a cheap Google TV dongle running the SPA build
      in Chrome gives a real number for the Phase D budget. Guessing is how TV apps ship slow.

### H3. Backend candidates for Android playback (research, not commitment)

- **Media3 / ExoPlayer via a Tauri Android plugin** — the platform-native answer, hardware
  decoding, and what every Android TV app uses.
- **libmpv on Android** — keeps one player codebase; buildable but heavy, and the surface
  embedding problem is different rather than solved.
- **`tauri-plugin-videoplayer`** — an existing v2 plugin that launches a native player from the
  webview; worth evaluating before writing one.

The torrent side (`librqbit` + the axum stream server in `torrent/stream_server.rs`) is
architecturally portable — it is a local HTTP server, and Android can run it. That part needs
verification, not redesign.

---

<a id="global-acceptance"></a>

## Global acceptance checks


- [ ] `pnpm check` clean, including the new 500-line rule.
- [ ] `pnpm test` clean; new pure-logic tests for `focus-geometry.ts`, the theme resolver, the mode
      resolver, and the paginated `browse()`.
- [ ] `pnpm build` clean; entry chunk under 300 KB uncompressed; no route chunk over 150 KB.
- [ ] Playwright covers: theme toggle round-trip, mode toggle round-trip, a full D-pad journey
      (home → rail → card → detail → play → back, arrow keys only), and admin being unreachable in
      Display Mode.
- [ ] Manual: the whole app driven with **only** arrow keys, Enter, and Escape, in Display Mode, on
      the real Tauri build, from the couch.
- [ ] Manual: light mode reviewed page by page.
- [ ] Every screen has exactly one visible selection at all times.

---

<a id="ideas-deferred"></a>

## Ideas considered and deliberately deferred

Recorded so they are not re-litigated mid-release.

**Worth doing later, not now:**

- **View transitions** for card → detail navigation. `startViewTransition` would make the Big
  Picture feel land properly, but it is a Chromium-version gamble on the Android System WebView.
  Revisit after H2e gives a real device to test on.
- **A "now playing" persistent bar** in Display Mode.
- **Voice search** via the Android TV remote's mic.
- **A screensaver / ambient mode** — poster slideshow after N minutes idle. Cheap to build, very
  much the Big Picture feel, but it is polish.
- **Per-profile home layouts** (which rails, in what order).
- **Offline-first with a service worker.** The offline feature today is per-title file transfer;
  a real offline-capable catalog is its own release.

**Rejected:**

- **A separate `apps/tv` package.** Two apps means two of every bug fix. The mode contract in B1
  gets the same result with one codebase.
- **Rewriting the player.** `player-page.tsx` and the Rust side are the best-engineered part of
  this repo — rAF-extrapolated playhead, refs instead of state, throttling in Rust before the IPC
  boundary. Adopt the shared focus tokens and otherwise leave it alone.
- **Replacing Tailwind or shadcn.** The problem is discipline, not the tools.
- **Full i18n.** Arabic-first is a product decision, not an oversight.
- **Server-side rendering the web app.** The Tauri constraint that produced `spa` mode is real and
  documented in `vite.config.ts:53-58`; nothing in v3.2 changes it.

---

## References

- [Navigation on TV — Android Developers](https://developer.android.com/design/ui/tv/guides/foundations/navigation-on-tv)
- [TV navigation — Android Developers](https://developer.android.com/training/tv/get-started/navigation)
- [Optimizing navigation for TV — Android Developers](https://stuff.mit.edu/afs/sipb/project/android/docs/training/tv/optimizing-navigation-tv.html)
- [Tauri: additional settings needed for Android TV (issue #10534)](https://github.com/tauri-apps/tauri/issues/10534)
- [Tauri webview versions](https://v2.tauri.app/reference/webview-versions/)
- [tauri-plugin-videoplayer](https://github.com/yeonv/tauri-plugin-videoplayer)
- Prior Arcadia roadmaps: [`v0.2-roadmap.md`](./v0.2-roadmap.md), [`v0.3-roadmap.md`](./v0.3-roadmap.md),
  [`player-torrent-roadmap.md`](./player-torrent-roadmap.md),
  [`deployment-and-release-roadmap.md`](./deployment-and-release-roadmap.md)
